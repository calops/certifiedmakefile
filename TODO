* Create a branch 'all-languages' to handle this case without breaking the global system
* Create a branch 'unit-test' where the Makefile is tested using the
  fixed-point method
* Make TODO list redundant with the issues on GitHub
